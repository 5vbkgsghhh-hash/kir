"""Numbers for translating free-form input into an editable BIM structure.

The package does NOT recognize meaning, does not edit the project, and does
not read storage: it measures the discrepancy between ONE source body and
MANY derived ones and names the remainder by address and number. An
element's role remains an authorial assertion (`kir/project_refinement.py`);
the measurement lives separate from the assertion on purpose.
"""
