"""Как проектируют — отдельно от того, чем проектируют.

`kukai.ir` — это ЯЗЫК: операции, компилятор, заземление, постусловия. Он отвечает
на вопрос «корректна ли эта программа» и ничего не знает о зданиях.

Этот пакет — ПРОЦЕСС поверх языка. Он отвечает на другой вопрос: «получилось ли
здание», и живёт отдельно намеренно. Причина не в опрятности: у них разные
критерии правды. Компилятор обязан быть консервативным и обратно совместимым —
его отказы это контракт. Проверки процесса выводятся из замеров и будут меняться
каждый раз, когда обнаружится новый способ построить формально верную чепуху
(за 27–28.07 таких нашлось три). Смешать их — значит либо заморозить процесс,
либо расшатать язык.

Границу держат три правила:

* сюда можно импортировать `kukai.ir`, обратно — нельзя;
* весь пакет выключается флагом `KUKAI_MODEL_REVIEW=0`, и тогда поведение
  системы байт-в-байт прежнее;
* в остальном коде от него ровно две строки — запись программы в
  `ir/serving.py` и хук ревью в `llm/client.py`.

Что внутри:

* `parti`     — скелет проекта: одно состояние, из которого все разделы ВЫВОДЯТ
                геометрию вместо того, чтобы выбирать её заново;
* `coherence` — проверки трёх охватов: пары элементов, план как план,
                соответствие построенного объявленному скелету;
* `review`    — накопитель программ хода и формулировки находок для модели;
* `practice.md` — сам процесс словами: скелет, стадии с воротами, правило
                встречных требований, список невыразимого.
"""
