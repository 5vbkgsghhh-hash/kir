"""Tier 1 templates — Jinja2 C# templates with YAML manifests.

Loaded by TemplateRegistry. Not imported as Python; the directory is just
a discovery root for `*.cs.j2` and `*.manifest.yaml` pairs.
"""
