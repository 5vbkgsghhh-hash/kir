"""Живой поток замысла — ЧИТАТЕЛИ журнала программ, и ничего кроме.

Пакет намеренно пуст: `__init__` не импортирует ни одного модуля. Это не
аккуратность, а несущая конструкция. `kukai/ir/__init__.py` импортирует
компилятор, поэтому любой модуль ВНУТРИ `kukai.ir` тянет компилятор одним
фактом своего расположения — и обход импортов не смог бы отличить «рисовальщик
зовёт компилятор» от «рисовальщик лежит рядом с компилятором».

Здесь этой двусмысленности нет по построению: `kukai.live` не тянет ничего, и
единственное ребро отсюда в компилятор — ленивый импорт `kukai.ir.preview`
внутри рабочего тела. Сам `preview.py` на уровне модуля не импортирует из
`kukai.ir` НИЧЕГО (замер: только stdlib). Проверяется машинно —
`kukai/ir/tests/test_live_plan_stream.py::test_no_reverse_edge_into_compiler`.

ПЯТЬ МОДУЛЕЙ И ПОЧЕМУ ИХ ПЯТЬ, А НЕ ДВА (04.08)
-----------------------------------------------
`journal` (что заявлено) и `plan_stream` (как это выглядит) — путь ТУДА, и он
обязан остаться односторонним. `showroom`, `transfer` и `verdict` — путь
ОБРАТНО, и он разрезан ровно по этой границе:

  * `showroom` хранит то, что показали, под подписью содержимого. Он
    stdlib-ЛИСТ, потому что его наполняет `plan_stream`: приобрети витрина
    путь в компилятор — и односторонность потока стала бы ложной ЧЕРЕЗ НЕЁ;
  * `transfer` решает, что переносить, и потому знает и реестр операций, и
    превью. Его не импортирует НИ ОДИН модуль пути «туда» — только `chat_ws`.
    Проверяется машинно (`test_kir_transfer.py::
    test_transfer_is_not_imported_by_the_drawing_path`);
  * `verdict` (04.08) судит накопленную пачку и отдаёт вердикт о ЗДАНИИ в
    квитанцию МОДЕЛИ. Тоже путь обратно и по тем же правилам: знает вердикт
    (то есть компилятор), не импортируется ни одним модулем пути «туда», и
    единственный его импортёр — `kukai/ir/serving.py`. До него пачка уезжала
    ЧЕЛОВЕКУ (`showroom`) и REVIT'У (`transfer`), а судье — никогда.

Иначе говоря: рёбер обратно три, каждое названо, и ни одно не ведёт внутрь
пути «туда».
"""
