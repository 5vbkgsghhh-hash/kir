"""ВЬЮЕР ЗДАНИЙ — то, ради чего существует всё остальное.

Пакет намеренно НЕ импортирует ничего на уровне модуля, ровно по тому же
доводу, что `kukai/live/__init__.py`: `kukai.ir.__init__` тянет компилятор,
поэтому любой модуль внутри `kukai.ir` тащит компилятор одним фактом своего
расположения. Вьюер лежит отдельным пакетом и берёт компилятор ТОЛЬКО там,
где действительно судит компилируемость (`compilability`), а геометрию —
ТОЛЬКО ленивым импортом `clash` (`scene`).

ПЯТЬ МОДУЛЕЙ И ПОЧЕМУ ИХ ПЯТЬ:

  * `honesty`  — словарь доверия и точности; ни одного рисования, ни одного
    импорта тяжёлого. Он ЛИСТ, и это несущее: его читают и сцена разбора, и
    сцена живого журнала, и они обязаны говорить об одном и том же одними
    словами. Два словаря честности разъехались бы молча;
  * `codec`    — байты сцены. Только stdlib. Ни `clash`, ни `ir`;
  * `scene`    — РАЗОБРАННОЕ здание: оболочки `clash` + `tree.json`;
  * `live_scene` — ЗАЯВЛЕННОЕ здание: программы `kukai.live.journal`,
    экструдированные через `preview`. Включая незакоммиченное в Revit;
  * `compilability` — непрерывный индикатор «это сейчас скомпилируется»
    вместе с полным списком того, чего он НЕ проверяет.

ОДИН ЭКРАН, ДВА ИСТОЧНИКА. `scene` и `live_scene` кодируют ОДИН формат и
пользуются ОДНИМ словарём честности, поэтому «открыть существующую модель» и
«смотреть, как модель пишут» — это один экран, а не два. Разница едет полем
`assertion`: `independent` у разбора, `self_reported` у журнала. Клиент
обязан показывать её, а не выводить.

ГРАНИЦЫ. В `kukai/clash/**` не написано ни строки: там работает другой агент,
и оттуда только читается. `kukai/ir/serving.py`, `spec.py`, `ops_*.py`,
`authoring.py`, `decompile/**` — тоже только чтение.
"""
